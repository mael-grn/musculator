

export default function PageLoading() {

    return (
        <div className={"flex w-full h-full items-center justify-center"}>
            <img className={"w-10 h-10"} src={"/icons/loader.gif"} alt={"loader"}/>
        </div>
    );
}